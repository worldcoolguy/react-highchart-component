import React from 'react';
import ReactDOM from 'react-dom';
import Chart4 from './Chart4';

it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<Chart4 />, div);
});
